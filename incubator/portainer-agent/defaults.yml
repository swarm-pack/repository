---
service_name: portainer_agent
image:
  repository: portainer/agent
  tag: 1.2.1
  tag_pattern: ^1.2.1

deploy:
  mode: global
  placement_constraints: []
swarm-sync:
  managed: true

host_port: 9001
agent_secret: changeme

networks:
  default:
    driver: overlay
    attachable: true